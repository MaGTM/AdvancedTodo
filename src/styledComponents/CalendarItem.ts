import {Box, styled} from "@mui/material";

export const CalendarItem = styled(Box)({
    color: '#fff',
    height: '100px',
    display: 'flex',
    padding: 10,
    cursor: 'pointer',
    borderRadius: '10px',
})